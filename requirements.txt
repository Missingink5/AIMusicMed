# AI 冥想助手 - 依赖包列表
# 安装命令: pip install -r requirements.txt

###############################################
# 最小运行依赖 (兼容国内镜像 & GPU 环境)
# 说明:
# - 去掉 torchaudio / fastapi / pydub 等未使用组件
# - librosa==0.10.1 仍依赖 numpy < 2.0 与 scipy <2；镜像只有最高 scipy 1.15.x，
#   因此将 numpy 约束为 <2.0 以避免 pip 去解析 scipy>=1.16.0 的新组合。
# - 不显式写 scipy；pip 会为 librosa 自动选取兼容版本 (<=1.15.3)。
###############################################

# 核心 AI
openai>=1.98.0
transformers>=4.53.3
torch>=2.7.0

# 音频与 TTS
librosa==0.10.1
soundfile>=0.12.1
edge-tts>=7.0.2

# 工具
numpy>=1.24,<2.0
requests>=2.32.0
tqdm>=4.67.1
typing_extensions>=4.8.0

# 可选(需要时再添加):
# fastapi uvicorn accelerate huggingface-hub pydantic
